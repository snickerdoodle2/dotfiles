{
	"context": {
        "prefix": "ctx, cancel",
        "description": "Create a context with a timeout and a cancel function",
		"body": [
            "ctx, cancel := context.WithTimeout(context.Background(), $0*time.Second)",
            "defer cancel()"
		]
	}
}
